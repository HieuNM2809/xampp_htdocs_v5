# MarkItDown core + tất cả converter (PDF, DOCX, PPTX, XLSX, audio, YouTube, ...)
markitdown[all]>=0.0.2

# HTTP service (ví dụ 04)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Chỉ dùng để chạy make_samples.py (sinh file mẫu .docx, .pdf).
# MarkItDown đọc DOCX bằng mammoth, không cần python-docx để chạy demo.
python-docx>=1.1.0
reportlab>=4.0.0
